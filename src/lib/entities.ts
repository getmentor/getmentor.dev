/**
 * Entity symbols for secure/hidden fields
 * These are used to prevent sensitive data from being serialized to JSON
 */

export const AUTH_TOKEN = Symbol('authToken')
export const CALENDAR_URL = Symbol('calendar_url')
